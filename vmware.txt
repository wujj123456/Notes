vmware

/* NAT ssh configuration */
https://superuser.com/questions/244324/how-can-i-set-it-up-so-i-can-ssh-into-my-vmware-guest-vm
Edit C:\ProgramData\VMware\vmnetdhcp.conf or /etc/vmware/vmnet8/dhcp/dhcp.conf
```
# Just like a normal dhcpd reservation
host my_vm_1 {
     hardware ethernet *your_vm_eth_mac_here**;
     # This is in default range for guests, but outside the 'pool'.
     # - check that it matches your vmware config
     fixed-address 192.168.198.10;
     # And/or your own internal nameserver etc
     option domain-name-servers 8.8.8.8;
     option domain-name "some_domain_suffix";
     # This is the default default-route of the VM
     option routers 192.168.198.2;
}
```
Edit C:\ProgramData\VMware\vmnetnat.conf or /etc/vmware/vmnet8/nat/nat.conf
```
[incomingtcp]
22222 = 192.168.198.10:22
```

/* shrink/compat disk for Linux guest VM */
vmware-toolbox-cmd disk shrink /

/* enable more memory to be used by VM */
https://petri.com/fixing-enough-physical-memory-available-errors-vmware-workstation/
C:\ProgramData\VMware\VMware Workstation\config.ini
prefvmx.allVMMemoryLimit
prefvmx.minVmMemPct

/* disable side-channel mitigation */
Edit .vmx
ulm.disableMitigations="TRUE"

/* guest add-ons */
https://wiki.archlinux.org/title/VMware/Install_Arch_Linux_as_a_guest
pacman -Sy open-vm-tools
systemctl enable vmtoolsd
systemctl enable vmware-vmblock-fuse
# reboot or start service

/* shared folders in Linux */
https://knowledge.broadcom.com/external/article/313233/how-to-configure-vmware-tools-shared-fol.html
# list available folders
vmware-hgfsclient
# mount
mkdir /mnt/whatever
vmhgfs-fuse -o allow_other -o auto_unmount .host:/ /mnt/whatever
/etc/fstab
.host:/ /mnt/hgfs fuse.vmhgfs-fuse defaults.allow_other 0 0

/* uninstall on Linux */
vmware-installer -u vmware-player

/* UEFI firmware */
https://www.serverwatch.com/server-tutorials/enabling-uefi-on-virtual-machines.html
Edit .vmx and add
firmware = "efi"
